# 🤖 ANDO.5 مع نظام الذكاء الاصطناعي

منصة تعليمية ذكية بنظام محادثة AI متقدم

## ✨ الميزات الجديدة

### 🤖 Chatbot ذكي
- محادثة ثنائية الاتجاه مع الذكاء الاصطناعي
- فهم اللغة الطبيعية (NLP)
- توصيات ذكية شخصية
- سجل محادثات كامل

### 📚 قاعدة معارف شاملة
- معلومات مفصلة عن 3 لغات برمجة (Python, JavaScript, C++)
- موارد تعليمية موثوقة
- اقتراحات ذكية حسب المستوى والاهتمامات

### 🔒 أمان عالي
- Content Security Policy (CSP)
- Input Validation و Sanitization
- منع XSS و CSRF
- CORS محمي

---

## 🚀 التثبيت والتشغيل

### 1️⃣ تثبيت المتطلبات

```bash
# تثبيت مكتبات Python المطلوبة
pip install -r requirements.txt
```

### 2️⃣ تشغيل الخادم

```bash
# تشغيل Flask Server (يجب أن يعمل في terminal منفصلة)
python AI.py
```

الخادم سيعمل على: `http://localhost:5000`

### 3️⃣ فتح الموقع

ثم افتح `index.html` في متصفحك (في terminal منفصلة أو عبر live server)

---

## 📁 هيكل المشروع

```
ANDO.5/
├── index.html          # الصفحة الرئيسية
├── style.css           # أنماط الموقع
├── script.js           # منطق النموذج والتحقق
├── chatbot.css         # أنماط الـ Chatbot
├── chatbot.js          # منطق الـ Chatbot
├── AI.py              # نظام الذكاء الاصطناعي (Flask Backend)
├── requirements.txt    # مكتبات Python المطلوبة
└── README.md          # هذا الملف
```

---

## 🔌 نقاط API المتاحة

### 1. فحص الصحة
```
GET /api/health
```
التحقق من أن الخادم يعمل بشكل صحيح.

### 2. المحادثة الذكية
```
POST /api/chat
Content-Type: application/json

{
  "message": "نص السؤال"
}
```

**الرد:**
```json
{
  "status": "success",
  "confidence": 0.95,
  "intent": "language_info",
  "message": "الرد الذكي",
  "data": {
    "description": "...",
    "uses": [...],
    "resources": [...]
  }
}
```

### 3. معلومات اللغة
```
POST /api/language-info
Content-Type: application/json

{
  "language": "python"
}
```

### 4. التوصيات الذكية
```
POST /api/recommend
Content-Type: application/json

{
  "name": "أحمد",
  "language": "python"
}
```

### 5. الاقتراحات
```
GET /api/suggestions
```

### 6. السجل
```
GET /api/history
```

---

## 🎮 كيفية الاستخدام

### من الموقع:
1. اضغط على الزر 💬 في أسفل اليمين
2. اكتب سؤالك أو اختر من الاقتراحات
3. استقبل الإجابة الذكية والتوصيات

### أمثلة أسئلة:
- "ما هي Python؟"
- "ايهما أفضل؟"
- "معلومات عن JavaScript"
- "أيهما لغة سهلة؟"
- "توصية لي"

---

## 🛠️ التطوير والتخصيص

### إضافة لغة برمجة جديدة

عدّل `AI.py` وأضف في `KNOWLEDGE_BASE`:

```python
"rust": {
    "description": "لغة برمجة آمنة وسريعة",
    "uses": ["البرامج النظام", "..."],
    "resources": [...],
    "difficulty": "صعبة",
    "popularity": "⭐⭐⭐⭐"
}
```

### تحسين الـ NLP

يمكنك إضافة مكتبات متقدمة:
```bash
pip install nltk spacy
```

---

## 🔐 ملاحظات الأمان

✅ **مفعل:**
- Content Security Policy (CSP)
- Input Validation للنصوص
- CORS محمي
- منع XSS attacks
- Sanitization للمدخلات

⚠️ **للإنتاج:**
- استخدم HTTPS بدلاً من HTTP
- أضف Authentication
- استخدم قاعدة بيانات بدلاً من الذاكرة
- أضف Rate Limiting
- استخدم gunicorn بدلاً من Flask dev server

---

## 📊 الإحصائيات

- **لغات مدعومة**: 3 (Python, JavaScript, C++)
- **نقاط API**: 6
- **رسائل افتراضية**: 5 أنواع
- **استجابة سريعة**: < 100ms
- **دعم الفهرسة العربية**: كامل ✅

---

## 🐛 استكشاف الأخطاء

### الخادم لا يعمل
```bash
# تأكد من Python 3.7+
python --version

# أعد تثبيت المكتبات
pip install --upgrade flask flask-cors

# تشغيل مع التفاصيل
python AI.py --debug
```

### الـ Chatbot لا يظهر
- افتح Developer Tools (F12)
- تحقق من Console من الأخطاء
- تأكد أن `chatbot.js` و `chatbot.css` محملة

### لا توجد استجابة من الـ AI
- تأكد من تشغيل الخادم: `http://localhost:5000/api/health`
- تحقق من أن الرسالة ليست فارغة
- جرب إعادة تحميل الصفحة

---

## 📝 الترخيص

تم إنشاؤه بواسطة: Mohamed Mostafa
الإصدار: 1.2 (beta) + AI

---

## 🤝 المساهمة

نرحب بأفكارك وتحسيناتك! 
يمكنك:
- إضافة لغات برمجية جديدة
- تحسين استجابات الـ AI
- إضافة ميزات جديدة
- تصحيح الأخطاء

---

## 📞 التواصل

للأسئلة والاقتراحات، تواصل معنا على:
📧 Email: contact@ando5.com
🔗 Website: https://ando5.com

---

**صُنع بـ ❤️ و 🤖 من قبل فريق ANDO.5**
